# Credenciais e chaves do Google (se houver no futuro)
credentials.json
*.json

# Ambientes virtuais e arquivos do Python
__pycache__/
*.pyc
.venv/
venv/
.env

# Cache local de faturamento sincronizado com a Cardápio Web
admfood.db
sincronizar.log
completar_pedidos*.log
flask_dev.log

# Fotos de produtos do Cardápio, subidas pela tela — dado local/de produção,
# não código (em produção fica no volume persistente, não aqui)
cardapio_fotos/

# Arquivos do VS Code
.vscode/